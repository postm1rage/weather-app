class WelcomeScreen {
  constructor(container, onComplete) {
    this.container = container;
    this.onComplete = onComplete;
  }

  render() {
    throw new Error('пока ничего');
  }

  hide() {
    throw new Error('але оп');
  }
}

export default WelcomeScreen;